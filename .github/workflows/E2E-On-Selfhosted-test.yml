name: Self-Hosted Runner Example

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run a simple test
      run: |
        cd appium
        #npm install --save-dev @wdio/cli --no-wizard
        #npm ci
        npm run test
