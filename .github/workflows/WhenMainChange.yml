name: Build Android

on:
  pull_request:
    types: [opened,reopened,edited,synchronize]
    branches: [ "main" ]
    paths:
      - '**'
  workflow_dispatch:

jobs:
  test:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest ]
        Build-ENV: [ 'QA', 'PROD' ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: pepe test
        run: ls -Rv ./.github
      - name: Build & Upload to OTA
        uses: ./.github/actions/buildAndUpload/action.yml
        with:
          Build-ENV: ${{ matrix.build-ENV }}