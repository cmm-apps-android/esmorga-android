name: Build Android
run-name: ${{ github.actor }} is testing a github action ${{ github.ref }} new feature

on:
  pull_request:
    types: [opened,reopened,edited,synchronize]
    branches: [ "main" ]
    paths:
      - '**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    #    runs-on: ${{ matrix.os }}
#    strategy:
#      matrix:
#        os: [ ubuntu-latest ]
#        Build-ENV: [ 'QA', 'PROD' ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: pepe test
        run: ls -Rv ./.github
      - name: Build & Upload to OTA
        uses: ./.github/actions/buildAndUpload/action.yml
        with:
          Build-ENV: 'QA'